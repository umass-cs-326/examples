// We include this line to start the strict mode, which helps catch errors
// and prevents bad code from running.
"use strict";

// Here are a few examples of things it catches:

bad = "this is a bad global variable!";

NaN = 3.14;

function garbage(x, x) {
  // This is a function that does nothing.
}
